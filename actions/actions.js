module.exports.save_smoker_words = function save_smoker_words(smoker_words) {
    console.log(`Storing ${smoker_words} to database`)
};

module.exports.save_mover_words = function save_mover_words(mover_words) {
    console.log(`Storing ${mover_words} to database`)
};